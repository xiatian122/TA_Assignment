<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">TA Assignment</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if logged_in? %>
          <li class="<%= 'active' if current_page?(user_path(current_user)) %>"><%= link_to("Profile", current_user)%></li>

          <% if can? :manage, ApplicationPool %>
            <li class="<%= 'active' if current_page?(application_pools_path) %>"><%= link_to("Application Pools", application_pools_path)%></li>
          <% end %>

          <% if can? :manage, StudentApplication %>
            <li class="<%= 'active' if current_page?(student_applications_path) %>"><%= link_to("Student Applications", student_applications_path)%></li>
          <% end %>

          <% if can? :manage, Course %>
            <li class="<%= 'active' if current_page?(courses_path) %>"><%= link_to("Courses", courses_path)%></li>
          <% end %>

          <% if can? :manage, User %>
            <li class="<%= 'active' if current_page?(users_path) %>"><%= link_to("Users", users_path)%></li>
          <% end %>

          <li><%= link_to "Log out", logout_path, method: "delete" %></li>

        <% else %>
          <li class="<%= 'active' if current_page?(login_path) %>"><%= link_to "Log in", login_path %></li>
        <% end %>
      </ul>

    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>